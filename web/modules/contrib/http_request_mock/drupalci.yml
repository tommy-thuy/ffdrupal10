build:
  assessment:
    validate_codebase:
      phplint:
        halt-on-fail: true
      phpcs:
        sniff-all-files: false
        halt-on-fail: true
    testing:
      run_tests.kernel:
        types: 'PHPUnit-Kernel'
        testgroups: '--all'
        suppress-deprecations: false
        halt-on-fail: false
